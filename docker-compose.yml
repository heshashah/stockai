version: "3.9"

services:

  frontend:
    build: ./client
    ports:
      - "3000:3000"
    depends_on:
      - backend

  backend:
    build: ./server
    ports:
      - "5000:5000"
    depends_on:
      - python

  python:
    build: ./python
    ports:
      - "8000:8000"
